{
  "_comment": [
    "Accessibility scanning rules. The page list is generated from",
    "_site/sitemap.xml by scripts/build_pa11y_config.py, not kept here.",
    "To skip a page, add it to `exclude` with a reason. Keep that short:",
    "every entry is a page nobody is checking."
  ],
  "defaults": {
    "standard": "WCAG2AA",
    "timeout": 60000,
    "chromeLaunchConfig": {
      "args": [
        "--no-sandbox",
        "--disable-setuid-sandbox",
        "--disable-dev-shm-usage"
      ]
    }
  },
  "exclude": []
}
